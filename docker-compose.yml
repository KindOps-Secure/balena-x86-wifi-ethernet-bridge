version: '2.1'

services:
  bridge:
    build: ./bridge
    network_mode: host
    privileged: true
    labels:
      io.balena.features.sysfs: '1'
      io.balena.features.procfs: '1'
      io.balena.features.kernel-modules: '1'
    restart: always
